using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arena2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        MainMenu.CharacterTwo = 2; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
